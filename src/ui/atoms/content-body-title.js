import styled from 'styled-components'

const ContentBodyTitle = styled.h2`
  font-family: 'Cabin', sans-serif;
  font-size: 36px;
  font-weight: 400;
  margin: 5px 0px;
  color: #4B0013;
  `

export default ContentBodyTitle