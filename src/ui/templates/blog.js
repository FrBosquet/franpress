import React from 'react'

import { Content, Header, Navigation, Interaction } from '../organisms'

const Blog = () => (
	<div>
		<Header />
		<Content />
		<Navigation />
		<Interaction />
	</div>
)

export default Blog
